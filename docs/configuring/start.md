# 🧩 Hyprland — Configuração Inicial (Start)

A configuração do **Hyprland** por padrão está localizada em:

```bash
$XDG_CONFIG_HOME/hypr/hyprland.conf
```

Na maioria das distribuições Linux, isso significa:

```bash
~/.config/hypr/hyprland.conf
```

---

## ⚙️ Usando um arquivo de configuração específico

Caso queira usar um arquivo de configuração que **não esteja localizado no diretório padrão**, utilize a seguinte sintaxe:

```bash
Hyprland [--config | -c] /caminho/para/seu/arquivo.conf
```

> 💡 Com o argumento `--config` (ou `-c`), você pode instruir o Hyprland a iniciar utilizando o arquivo especificado.

---

## 🧠 Geração automática de configuração

O Hyprland **gera automaticamente** um arquivo `hyprland.conf` em `~/.config/hypr/` caso você ainda não possua um.

Assim que abrir esse arquivo, é recomendável **remover a linha abaixo**, responsável pela geração automática:

```conf
autogenerated = 1
```

---

## 🔄 Recarregando a configuração

A configuração do Hyprland é **recarregada automaticamente** assim que você salva o arquivo.
Para garantir que as alterações foram aplicadas, você também pode recarregar manualmente com o comando:

```bash
hyprctl reload
```

---

## 🧱 Estrutura de seções

Inicie uma seção com o **nome** e termine com o **bloco** `{}`.

> Lembre-se de que cada propriedade dentro do bloco deve estar em **linhas separadas**.

**Exemplo:**

```conf
general {
    gaps_in = 5
    gaps_out = 20
    border_size = 2

    # https://wiki.hypr.land/Configuring/Variables/#variable-types — informações sobre cores
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)

    # Define se é possível redimensionar janelas clicando e arrastando nas bordas
    resize_on_border = false

    # Leia https://wiki.hypr.land/Configuring/Tearing/ antes de ativar esta opção
    allow_tearing = false

    layout = dwindle
}
```

---

## ⚠️ Observação importante

O arquivo de configuração padrão (`~/.config/hypr/hyprland.conf`) **não contém todas as opções possíveis** do Hyprland.
Ele serve apenas como um **exemplo básico** para você começar a personalizar.

Existem **muitas outras opções** (para animações, janelas, monitores, bordas, atalhos, etc.) que **não aparecem no arquivo padrão**.
Para consultar todas, acesse a [Wiki oficial do Hyprland](https://wiki.hyprland.org/).

