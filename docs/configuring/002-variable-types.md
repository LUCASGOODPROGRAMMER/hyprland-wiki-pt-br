## VARIABLE TYPES

### e.g. *(exempli gratia / por exemplo)*

- **int** — números inteiros.  
- **bool** — valores booleanos *(on/off, true/false, 0/1, yes/no)*.  
- **float** — números com casas decimais.  
- **color** — cor (veja abaixo as opções disponíveis).  
- **vec2** — vetor com 2 valores *float* separados por espaço *(e.g. `0 0` ou `1.2 3.4`)*.  
- **MOD** — string de teclas modificadoras *(modmask)*  
  *(e.g. `SUPER`, `SUPERSHIFT`, `SUPER + SHIFT`, `SUPER and SHIFT`, `CTRL_SHIFT`)*.  
- **str** — sequência de caracteres *(string)*.  
- **gradient** — gradiente de cores  
  *(sintaxe: `color color ... [angle]deg`)*.  
- **font_weight** — peso da fonte.  
  Aceita números inteiros de `100` a `1000` ou predefinições:  
  `thin`, `ultralight`, `light`, `semilight`, `book`, `normal`, `medium`, `semibold`, `bold`, `ultrabold`, `heavy`, `ultraheavy`.

---

### 🎨 Colors

Há três maneiras de definir cores:

- **rgba()** — e.g. `rgba(b3ff1aee)` ou o equivalente decimal `rgba(179,255,26,0.933)`  
  *(valores decimais não devem conter espaços)*.  
- **rgb()** — e.g. `rgb(b3ff1a)` ou o equivalente decimal `rgb(179,255,26)`.  
- **legacy** — e.g. `0xeeb3ff1a`, na ordem **ARGB**.

---

### ⌨️ Mod List *(lista de teclas modificadoras / modmask)*

`SHIFT`, `CAPS`, `CTRL` / `CONTROL`, `ALT`, `MOD2`, `MOD3`, `SUPER` / `WIN` / `LOGO` / `MOD4`, `MOD5`

